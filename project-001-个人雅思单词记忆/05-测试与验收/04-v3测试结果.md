# v3 测试结果

测试日期：2026-06-22

## 已通过

- v2 本地数据自动迁移至 v3，原单词和学习状态保留。
- 示例词 `substantial` 自动获得人工整理的词根、联想、词族和音节数据。
- 新词学习页显示5种记忆法；词库“记忆法”入口显示相同内容。
- 编辑单词可修改4类记忆辅助字段。
- 保存 YouTube 链接后生成实际 iframe 播放器，地址包含开始和结束秒数。
- 片段刷新后仍保留；每词最多3段。
- 30秒片段被拒绝，并显示“片段长度必须是5–20秒”。
- 第4段被拒绝，并显示“每个单词最多保存3个片段”。
- 页面运行无控制台错误。
- GitHub macOS runner 构建成功，最终 IPA 大小为77,777字节。
- IPA 包内确认存在 v3 数据、五种记忆法、视频播放器、片段限制和 v4 PWA 缓存。
- IPA 显示名仍为“单词TUO”，Bundle ID 仍为 `com.personal.wordtuo`。

## 视觉证据

- `mobile-v3-memory-methods.png`
- `mobile-v3-video-context.png`

## 明确边界

- PlayPhrase.me 没有确认的免费公开接口，因此不自动抓取。
- 当前采用外部搜索后保存 YouTube 或公开直链，再在 App 内播放的方式。
