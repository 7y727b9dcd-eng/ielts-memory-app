# v13 朗读与记忆测试结果

日期：2026-07-03

## 本次范围

- 单词标题旁增加“听”按钮：学习卡片、复习卡片、词库列表。
- 修复发音回音：自动朗读改为单一调度器，播放前停止旧音频和系统朗读。
- 修复晚到播放：切换页面或阶段后，未完成的联网朗读请求会被令牌取消。
- 确认遗忘曲线定时复习：继续使用 SM-2 字段 `easeFactor`、`intervalDays`、`nextReview`。
- 扩展记忆方式：八种记忆法，新增句子联想、主动回忆、搭配语境、中英反向回忆等。
- PWA 资源版本升级到 v13。

## 验证结果

- `node --check 04-实现/web/app.js`：通过。
- `node --check 04-实现/web/sw.js`：通过。
- `node 05-测试与验收/check-v13-memory-audio.mjs`：通过。
- `node 05-测试与验收/check-v12-performance-lexicon.mjs`：通过。
- `node 05-测试与验收/check-catalog-feature.mjs`：通过，高中词库 2525 条。
- `node 05-测试与验收/check-android-apk-feature.mjs`：通过。
- 本地网页 `http://127.0.0.1:4181/index.html?v=13`：返回 200，加载 `app.js?v=13` 和 `styles.css?v=13`。
- 浏览器实测：首页可打开，学习页显示单词旁“听”按钮，记忆法数量为 8，页面无白屏。
- GitHub Actions 安卓 APK 构建：成功。
- 最终 APK：`07-交付物/WordTUO-Android.apk`，SHA256 `0A6F7E58256F371D4703DB32626E7E0C27B1F6F91E6D3F12C91E12E15B743969`。
- APK 内部验包：包含 `assets/web/app.js`、`index.html`、`styles.css`、`sw.js`；`index.html` 使用 v13 资源；`sw.js` 缓存名为 `word-tuo-pwa-v13`；`app.js` 包含播放令牌和八种记忆法。

## 复习算法确认

- “不认识”：重置重复次数，`nextReview` 设为当天，进入继续学习。
- “模糊”：保留短间隔，按当前间隔约 1.2 倍安排下次复习。
- “认识”：按 SM-2 的重复次数和难度因子增加间隔，更新 `nextReview`。
- 到期复习队列只读取 `status !== "new"` 且 `nextReview <= today` 的单词。

## 结论

v13 网页功能和安卓 APK 均已通过验证。用户可安装 `07-交付物/WordTUO-Android.apk`。
