# v16 每日新词修复测试结果

日期：2026-07-13

## 问题

用户反馈“每日新词一直都是 0 个”。排查后发现旧逻辑用当天所有 `new-complete` 记录扣减每日新词额度，没有按当前词库隔离；当今天在其它词库学过新词后，切回当前词库也会被扣光额度。

同时，如果当前词库本身没有未学习单词，页面只显示 0，没有告诉用户应该切换词库、导入词库或添加单词。

## 修复

- 每日新词额度改为按当前学习词库扣减。
- `全部词库` 的词库匹配逻辑修正为匹配所有单词。
- 首页新增“刷新今日新词”按钮，会清除未完成的新词会话并重新计算今日新词。
- 当前词库没有未学习单词时，提示切换词库、导入词库或添加新单词。
- PWA 资源版本升级到 v16。

## 验证

- `node --check 04-实现/web/app.js`：通过。
- `node --check 04-实现/web/sw.js`：通过。
- `node 05-测试与验收/check-v16-daily-new-words.mjs`：7 项通过。
- `node 05-测试与验收/check-v15-memory-settings-onboarding.mjs`：通过。
- `node 05-测试与验收/check-v14-tap-feedback.mjs`：通过。
- `node 05-测试与验收/check-v13-memory-audio.mjs`：通过。
- `node 05-测试与验收/check-v12-performance-lexicon.mjs`：通过。
- `node 05-测试与验收/check-catalog-feature.mjs`：通过，高中词库 2525 条。
- `node 05-测试与验收/check-android-apk-feature.mjs`：通过。
- 本地预览 `http://127.0.0.1:4181/index.html?v=16`：HTTP 200。

## 待完成

- GitHub Actions 已重新构建安卓 APK，并复制到 `07-交付物/WordTUO-Android.apk`。
- APK SHA256：`BED58A17A8E95B870A2A9AB002F678C2661B6B5F15FBD14A0674D58F2B9339B1`。
- 已验包确认 APK 内置 `assets/web/index.html`、`app.js`、`styles.css`、`sw.js` 均为 v16，且包含 `refreshDailyNewWords`、`countTodayNewCompletedForActiveCatalog` 和 `showNoNewWordsGuidance`。
