# v17 学习页简化与视频入口测试结果

- 日期：2026-07-14
- 范围：学习页记忆法显示、设置页记忆法开关、无视频片段时隐藏视频入口、本地学习记录保留、PWA 缓存版本升级。

## 验证结果

- 通过：新增 `check-v17-learning-simplification.mjs`，先在旧实现下验证失败，再完成修复后验证通过。
- 通过：全量 `check-*.mjs` 回归检查全部通过。
- 通过：`app.js` 和高中词库脚本语法检查通过。
- 通过：本地预览 `http://127.0.0.1:4181/index.html?v=17` 返回 200。
- 限制：自动浏览器检查因当前依赖缺少 `playwright-core` 未执行，已用脚本语法、HTTP 可访问性和静态行为检查替代。

## 行为确认

- 旧学习记录继续保存在 `localStorage` 的 `word-tuo-state-v5`，本次升级不清空单词、学习历史、复习记录或考试记录。
- 默认记忆法从全量显示改为 4 个核心方法：词根词缀、音标拆读、句子联想、主动回忆。
- 旧版“全开默认”的用户数据会自动收敛为精简默认；用户在设置中手动选择后，会保留自定义组合。
- 学习页和单词记忆弹窗只显示设置中开启、且当前单词可用的方法。
- 没有已保存影视片段的单词不再显示影视语境入口，也不会在新词学习中进入视频阶段。
- 只有存在已保存视频片段且影视开关开启时，词库列表和学习流程才显示影视语境。
