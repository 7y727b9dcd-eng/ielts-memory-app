# v4 测试结果

测试日期：2026-06-22

## 已通过

- 新词完成认识和主动回忆后进入 `VIDEO CONTEXT`，页面不存在旧 `clozeForm`。
- 无片段时显示“查找并添加片段”和“本词先跳过影视”。
- 在学习中添加10秒 YouTube 片段并关闭窗口后，当前页面立即显示播放器。
- 播放器地址包含 `start=5` 和 `end=15`。
- 字幕 `The report shows a substantial increase.` 自动挖空目标词。
- 输入正确单词后显示“场景回忆正确”，并写入 `new-complete` 学习记录。
- 默认设置下进入影视阶段立即生成带 `autoplay=1` 的播放器。
- 关闭“进入影视语境时自动播放”后不创建 iframe，并保留手动播放按钮。
- 页面无控制台错误。
- GitHub macOS runner 最终构建成功，显示名修正版 IPA 大小为78,594字节。
- IPA 包内不存在新词阶段的 `EXAMPLE CLOZE`，确认包含影视阶段、自动播放和 v6 缓存。
- IPA 包内 `CFBundleDisplayName` 编码点为 `\u5355\u8bcdTUO`，主屏幕名称为“单词TUO”；Bundle ID 为 `com.personal.wordtuo`。
- 最终 SHA256：`024C5DE81E832DD6F1CB2AF28C71D73803D631C089C0AFD0486CBE103474CA60`。

## 视觉证据

- `mobile-v4-scene-empty.png`
- `mobile-v4-scene-learning.png`
